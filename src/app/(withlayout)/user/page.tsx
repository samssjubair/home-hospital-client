import { useGetMyInfoQuery } from '@/redux/api/userApi';
import React from 'react';

const UserPage = () => {
    // const {data, isLoading} = useGetMyInfoQuery({});
    // console.log(data);
    return (
        <div>
            This is User
        </div>
    );
};

export default UserPage;